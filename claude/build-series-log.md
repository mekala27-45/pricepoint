# Build series log

## Pricepoint

Public-data demand service with event-time credit accounting, temporal gold features, independent serving features, expanding-origin evaluation, calibrated intervals, immutable models, appended gate assessments, constrained pricing, shadow scoring and observed-time monitoring.

Reusable: verified dataset hashes; the PointInTimeFrame and ServingFeatureStore comparison; true-zero panel property tests; deliberate gate failures; prediction-led artifact verification; null-WAPE handling; delayed retraining scheduling; static/live client; six-job CI; container latency artifact workflow.

Finding: the demand candidate lost to the trailing-mean baseline and remained in shadow mode. Retail price coefficients were negative, which did not solve identification. Calendar-driven PSI triggers exposed the weakness of unconditional drift thresholds.

Scope: public UCI data only. No causal pricing uplift or production traffic claim. See RESULTS.md for measured values and current release evidence.
